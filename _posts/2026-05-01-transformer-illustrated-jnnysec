# 图解 Transformer：彻底理解现代大模型的基石 

> 原文：Jay Alammar — *The Illustrated Transformer* (2018, 更新至 2025)  
> 作者：jnnysec  
> 标签：Transformer, Deep Learning, LLM, Self-Attention

---

如果只推荐一篇理解 Transformer 的入门文章，Jay Alammar 的 *The Illustrated Transformer* 毫无疑问是首选。2018 年发布至今，它仍是斯坦福、哈佛、MIT 等高校深度学习课程的必读参考资料。这本身就是一件很说明问题的事——一篇博客能在学术圈被引用七年，靠的不是时效性，而是把复杂概念讲清楚的能力。

这篇文章是我通读它之后整理的学习笔记，加入了一个安全从业者视角下的理解。

---

## 一、为什么需要 Transformer？

2017 年之前，NLP 领域的主流架构是 RNN、LSTM、GRU。它们有一个共同的致命缺陷：**串行计算**。处理第 N 个词必须等前面 N-1 个词全部算完。这意味着 GPU 的大量并行计算单元在大部分时间里是空闲的。

CNN 虽然能并行，但卷积核的感受野有限。要捕捉长距离依赖关系，需要堆叠很多层——每层的计算量和参数量都在膨胀。

2017 年，Google 的 Vaswani 等人在 *Attention is All You Need* 中提出了一个激进的想法：**彻底拿掉循环结构**。不用 RNN，不用 CNN，全靠一种叫做「注意力」的机制来建模词与词之间的关系。Transformer 由此诞生。

结果？训练速度提升了数个量级。在机器翻译任务上的表现超越了之前所有模型。更重要的是，它开启了一条全新的技术路线——今天的所有大语言模型（GPT、Claude、Gemini、Llama、DeepSeek），无一例外都建立在 Transformer 架构之上。

---

## 二、整体架构：Encoder-Decoder

原论文的 Transformer 是一个经典的编码器-解码器结构（用于翻译任务）：

```
输入句子 → [Encoder × 6] → [Decoder × 6] → 输出句子
```

- **Encoder**：6 层堆叠，每层 = Self-Attention + Feed-Forward Neural Network
- **Decoder**：6 层堆叠，每层 = Self-Attention + Encoder-Decoder Attention + Feed-Forward

几个关键细节：

1. 6 层不是魔法数字——可以增减，原论文选了 6
2. 各层结构完全相同，但**不共享权重**
3. 每层的输入和输出维度恒定为 512（embedding 维度）
4. Feed-Forward 层对每个位置独立应用，完全相同——这意味着这部分可以完全并行

GPT 系列只用 Decoder 部分（Decoder-only），BERT 只用 Encoder 部分（Encoder-only）。理解原始架构后才能理解这些变体。

---

## 三、自注意力：Transformer 的灵魂

这是整篇文章最重要的概念。让我们从一个问题出发。

### 3.1 「它」到底指什么？

句子：*"The animal didn't cross the street because **it** was too tired."*

请问「it」指的是 animal 还是 street？人类秒懂——当然是 animal。但对计算机来说，这不是一个简单的问题。单词「it」的 embedding 向量本身不包含任何关于它指代什么的信息。

**Self-Attention 的解决思路**：在处理每个词时，让模型**同时查看输入中所有其他词**，从中找出与当前词最相关的那些，把它们的语义信息「烤」进当前词的表示中。

这跟 RNN 的隐藏状态思路类似，但 RNN 只能顺序传递信息（前面的词通过隐藏状态影响后面的词），而 Self-Attention 让**每个词直接与其他所有词建立联系**——不再有距离的概念。

### 3.2 Q、K、V：三个角色一张演出

具体机制：每个词被转换成三个向量——

| 向量 | 全称 | 直觉含义 |
|------|------|---------|
| **Q（Query）** | 查询 | 「我在找什么？」 |
| **K（Key）** | 键 | 「我有什么标签？」 |
| **V（Value）** | 值 | 「我实际携带什么信息？」 |

这三个向量都是从输入 embedding 通过三个不同的权重矩阵生成的，维度为 d_k = 64（embedding 512 ÷ 8 个头）。

### 3.3 计算过程：六步走

核心公式：

$$\text{Attention}(Q, K, V) = \text{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right) \times V$$

拆解成步骤：

1. 从输入 embedding 生成 Q、K、V 向量（每个输入词 → 三个 64 维向量）
2. 用当前词的 Q 去点乘所有词的 K，得到「相关性分数」——分数越高，两个词越相关
3. 将分数除以 √d_k（= 8）做缩放，防止点积值过大导致 softmax 梯度消失
4. Softmax 归一化，得到每个词对当前词的「注意力权重」（总和为 1）
5. 用注意力权重加权求和所有词的 V 向量
6. 输出一个新的向量（称为 z），它包含了来自上下文中所有相关信息

> **为什么除以 √d_k？** 假设 Q 和 K 的每个分量都是均值为 0、方差为 1 的随机变量，那么 QK^T 中每个值的方差是 d_k。当 d_k 很大时，点积值可能非常大，softmax 会变得极端（接近 one-hot），梯度趋近于零。除以 √d_k 把方差拉回 1，保证梯度正常流动。

---

## 四、多头注意力：八双眼睛看同一个世界

单头注意力的问题：它可能只捕捉到一种类型的关系——比如只关注代词指代，而忽略了动宾搭配。

Transformer 的解决方案：同时运行 **8 个独立的注意力头**，每头有自己的 Q/K/V 权重矩阵，并行计算后拼接结果。

不同头确实学到不同的模式：
- 有的头擅长追踪代词指代（it → animal）
- 有的头擅长捕捉动宾关系（cross → street）
- 有的头负责长距离语义依赖（段落级别的关联）

这种设计之所以优雅，不是因为人为指定了每个头的分工——分工是**训练过程中自动涌现的**。8 个头并行计算后拼接回 512 维，再通过一个额外的权重矩阵做线性变换，得到最终的输出。

---

## 五、位置编码：给模型装上「时钟」

拿掉 RNN 之后，模型失去了对词序的感知。「狗咬人」和「人咬狗」对模型来说变成了同样的词袋——因为 Self-Attention 本身不区分位置。

解决方案：在输入 embedding 上**加一个位置编码向量**。原论文使用正弦和余弦函数生成位置编码：

$$PE_{(pos, 2i)} = \sin\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)$$
$$PE_{(pos, 2i+1)} = \cos\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)$$

正弦/余弦函数的选择有两个好处：
1. 每个位置有唯一的编码
2. 相邻位置的编码相似（符合直觉——相邻词的语义关系更紧密）
3. 可以外推到训练时未见过的更长序列

后来的模型（如 GPT-3、LLaMA）更多使用可学习的绝对位置编码或 RoPE（旋转位置编码），但正弦函数方案奠定了理论基础。

---

## 六、残差连接与层归一化：深度网络的稳定器

每层 Self-Attention 和 Feed-Forward 之后都有两个固定操作：

- **Add（残差连接）**：把该层的输入直接加到输出上。数学上表示为 LayerNorm(x + Sublayer(x))。残差连接解决了深层网络的梯度消失问题——即使 Sublayer 的梯度很小，x 本身的梯度也能直接回传
- **Norm（层归一化）**：对每个样本的所有特征做归一化，稳定训练过程

这两个技术都不是 Transformer 发明的（残差网络来自 ResNet，层归一化是 2016 年的工作），但 Transformer 把它们用得恰到好处。

---

## 七、Decoder 的独特设计

Transformer 的 Decoder 有两处与 Encoder 不同的关键设计：

**1. Masked Self-Attention**

在生成第 N 个词时，Decoder 只能看到前 N-1 个词。具体实现是在 softmax 之前，把未来位置的注意力分数设为 -∞。这确保模型是「自回归」的——只能基于已生成的内容预测下一个词。这跟 GPT 系列的行为完全一致。

**2. Encoder-Decoder Attention**

Decoder 层的第二个 Attention 比较特殊：它的 Q 来自 Decoder 自己的上一层输出，但 K 和 V 来自 Encoder 的顶层输出。这意味着 Decoder 在生成每个词时都能「参考」整个输入句子的编码信息——这正是翻译任务的核心需求。

最后一层 Decoder 的输出经过一个线性变换（把维度从 512 映射到词表大小），再通过 softmax 得到词表上的概率分布。概率最高的那个词被选中作为输出。

---

## 八、关键数字

| 参数 | 值 | 说明 |
|------|-----|------|
| Encoder/Decoder 层数 | 6 | 可调整 |
| Embedding 维度 | 512 | d_model |
| 注意力头数 | 8 | 并行计算 |
| Q/K/V 维度 | 64 | d_k = d_v = 512 ÷ 8 |
| 前馈网络隐藏层 | 2048 | FFN 内部维度 |
| 英德翻译 BLEU | 28.4 | WMT 2014，当时 SOTA |
| 训练成本 | 基础模型的 1/3 ~ 1/50 | 相比此前的 SOTA 模型 |

---

## 九、从安全视角再看 Transformer

理解 Transformer 的内部机制对一个研究 LLM 安全的人来说有多重要？我的回答是：**这就是地基。**

- **Prompt Injection 为什么能工作？** 因为 Self-Attention 的机制是「所有词互相看」。恶意 Prompt 进入上下文窗口后，它的 Q、K、V 向量会参与每一个后续 token 的注意力计算。没有边界能把「数据」和「指令」分隔开——它们在同一个向量空间里。理解这个机制之后，你就理解了为什么 Prompt Injection 无法用传统输入过滤的方式解决。

- **间接注入为什么更危险？** 因为攻击内容不需要出现在用户的输入中——只要 Agent 检索到了，它就进入上下文窗口，然后像任何正常内容一样参与自注意力计算。自注意力没有「来源」的概念，它不知道这个 token 是用户写的还是网页上爬来的。

- **Decoder-only 与越狱**：现代 LLM 只保留 Decoder。理解 Masked Self-Attention 才能理解 LLM 的自回归性质，以及为什么越狱 prompt 可以通过构造特定的前缀来改变后续所有 token 的生成分布。

---

## 延伸阅读

- **原论文**：*Attention is All You Need*（Vaswani et al., 2017）——8 页，短小精悍
- **续篇**：Jay Alammar — *The Illustrated GPT-2*（理解 Decoder-only 架构）
- **动手实现**：Harvard NLP — *The Annotated Transformer*（PyTorch 逐行实现 + 注释）
- **升级版**：Jay Alammar — *Hands-On Large Language Models*（第 3 章，覆盖 2017-2025 的架构演进）
- **RoPE**：Su et al. — *RoFormer: Enhanced Transformer with Rotary Position Embedding*（2021，LLaMA/Qwen 采用的位置编码方案）

---

*本文基于 Jay Alammar 的 *The Illustrated Transformer* 整理，原文链接：https://jalammar.github.io/illustrated-transformer/*
